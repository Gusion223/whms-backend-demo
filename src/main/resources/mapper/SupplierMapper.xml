<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whms.mapper.SupplierMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.whms.entity.Supplier">
        <id column="sid" property="sid" />
        <result column="sname" property="sname" />
        <result column="saddr" property="saddr" />
        <result column="sphone" property="sphone" />
        <result column="sdesc" property="sdesc" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        sid, sname, saddr, sphone, sdesc
    </sql>

</mapper>
