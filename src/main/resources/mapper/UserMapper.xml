<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whms.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.whms.entity.User">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="nick_name" property="nickName" />
        <result column="password" property="password" />
        <result column="age" property="age" />
        <result column="sex" property="sex" />
        <result column="phone" property="phone" />
        <result column="user_type" property="userType" />
        <result column="isValid" property="isvalid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, nick_name, password, age, sex, phone, user_type, isValid
    </sql>
    <select id="pageC" resultType="com.whms.entity.User">
        select id, user_name, nick_name, age, sex, phone, user_type, isValid from user
    </select>

    <select id="getTotalCostSale" resultType="com.whms.entity.bo.TotalCostSale">
        SELECT total_cost, total_sale FROM (
           SELECT SUM(pototal_cost)  as total_cost FROM purchase_order
       ) a JOIN (
            SELECT  SUM(sototal) as total_sale FROM sale_order
        )b;
    </select>
</mapper>
