<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whms.mapper.PurchaseDetailMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.whms.entity.PurchaseDetail">
        <id column="poid" property="poid" />
        <result column="gid" property="gid" />
        <result column="pdamount" property="pdamount" />
        <result column="pdtotal_cost" property="pdtotalCost" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        poid, gid, pdamount, pdtotal_cost
    </sql>

</mapper>
